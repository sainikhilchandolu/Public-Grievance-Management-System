import AppRouter from './routes/AppRouter';

/**
 * Root App Component
 * Simply renders the router — all layout is handled by individual pages
 */
const App = () => {
  return <AppRouter />;
};

export default App;
